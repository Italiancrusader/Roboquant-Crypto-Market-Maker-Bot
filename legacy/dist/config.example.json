{
  "exchange": {
    "name": "bybit",
    "api_key": "YOUR_API_KEY_HERE",
    "api_secret": "YOUR_API_SECRET_HERE",
    "testnet": false,
    "comment": "Supported exchanges: binance, bybit, okx, kucoin, gate, mexc, bitget, hyperliquid, phemex, huobi, kraken"
  },
  
  "trading": {
    "symbol": "ETH/USDT:USDT",
    "leverage": 5,
    "order_size_type": "percentage",
    "order_size_percent": 0.01,
    "order_size": 0.001,
    "comment": "order_size_type can be 'fixed' or 'percentage'. If 'fixed', uses order_size. If 'percentage', uses order_size_percent of available balance"
  },
  
  "strategy": {
    "gamma": 0.1,
    "k": 1.5,
    "time_horizon": 1.0,
    "sigma_lookback": 100,
    "update_frequency": 1,
    "min_spread": 0.0001,
    "max_spread_percent": 0.01,
    "max_quote_distance_percent": 0.005,
    "comment": "gamma: risk aversion (0.01-1.0, lower=more aggressive), k: market impact (0.5-5.0), update_frequency: seconds between updates"
  },
  
  "risk": {
    "max_inventory_usd": 1000,
    "max_position_size_usd": 100,
    "stop_loss_percent": 0.05,
    "daily_loss_limit_usd": 50,
    "comment": "Risk management parameters to protect your capital"
  },
  
  "notifications": {
    "enabled": false,
    "telegram_bot_token": "",
    "telegram_chat_id": "",
    "email": "",
    "comment": "Optional: Get notifications about trades and errors"
  }
}
